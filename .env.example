# =============================================================================
# scan2wall Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED VARIABLES# -----------------------------------------------------------------------------

# Google Gemini API Key (for material property inference)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_gemini_api_key_here

# ComfyUI Configuration
# ComfyUI API URL (default: http://127.0.0.1:8188)
COMFY_URL=http://127.0.0.1:8188

# ComfyUI directories (auto-detected if not specified)
# COMFY_INPUT_DIR=/workspace/scan2wall/3d_gen/ComfyUI/input
# COMFY_OUTPUT_DIR=/workspace/scan2wall/3d_gen/ComfyUI/output

# Isaac Lab Configuration (Docker-based)
# Isaac Lab now runs in Docker containers
# Container path: /workspace/isaaclab (Isaac Lab installation inside container)
# Host mount: /home/ubuntu/scan2wall → /workspace/scan2wall (scan2wall project accessible in container)
ISAAC_WORKSPACE=/workspace/isaaclab
USD_OUTPUT_DIR=/home/ubuntu/scan2wall/isaac/usd_files

# -----------------------------------------------------------------------------
# OPTIONAL VARIABLES (Uncomment to customize)
# -----------------------------------------------------------------------------

# Upload Server Port
# Default: 49100
# Change if port is already in use or you need a different port
# PORT=49100

# Debug Logging
# Uncomment to enable detailed debug logging
# LOG_LEVEL=DEBUG

# -----------------------------------------------------------------------------
# PATH CONFIGURATION (Advanced - for custom deployments)
# -----------------------------------------------------------------------------
# These paths are auto-detected from the project structure by default.
# Only set these if you need custom paths or running on a single instance.

# Project Root Directory
# Default: Auto-detected from code location
# Where the scan2wall repository is cloned
# PROJECT_ROOT=/home/user/scan2wall

# Isaac Lab Workspace Directory (Docker container path)
# Default: /workspace/isaaclab
# This is the path INSIDE the Docker container where Isaac Lab is installed
# Do not change this unless you've modified the Docker setup
# ISAAC_WORKSPACE=/workspace/isaaclab

# Isaac Scripts Directory
# Default: ${PROJECT_ROOT}/isaac_scripts
# Where convert_mesh.py and test_place_obj_video.py are located
# ISAAC_SCRIPTS_DIR=/home/user/scan2wall/isaac_scripts

# Assets CSV File
# Default: ${PROJECT_ROOT}/assets.csv
# CSV file logging all generated assets and their properties
# ASSETS_CSV=/home/user/scan2wall/assets.csv

# Recordings Output Directory
# Default: ${PROJECT_ROOT}/recordings
# Where simulation videos are saved
# RECORDINGS_DIR=/home/user/scan2wall/recordings

# USD Output Directory
# Default: ${ISAAC_WORKSPACE}
# Where converted USD meshes are saved
# USD_OUTPUT_DIR=/home/user/isaaclab

# -----------------------------------------------------------------------------
# SINGLE MACHINE SETUP (Default Configuration)
# -----------------------------------------------------------------------------
# By default, scan2wall is configured to run on a single machine:
#
# - ComfyUI runs on port 8188
# - Upload server runs on port 49100
# - Isaac Lab runs in Docker containers (isaac-sim, isaac-lab)
# - Docker mounts:
#   • /home/ubuntu/scan2wall → /workspace/scan2wall (project files)
#   • /home/ubuntu/scan2wall/isaac/usd_files → /workspace/usd_files (USD outputs)
#   • /home/ubuntu/scan2wall/recordings → /workspace/recordings (videos)
#
# No additional configuration needed!
